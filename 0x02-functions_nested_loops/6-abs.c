#include "main.h"

/**
  * _abs - This function computes the absolute
  * value of an integer.
  *
  * @x: arguement value
  *
  *Return: Always 0 (success)
  */

int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
	{
		return (-x);
	}
}
